#Error, since syntax error doesn't need else clause to be run, they are found at parsing the code
#Had it been a name or type error it wouldn't have caused the Error because they are only checked at runtime
x = 2
if x == 2:
    print x
else:
    print x +
    


